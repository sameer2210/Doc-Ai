import type { ThemeMode } from './types';

export const THEME_STORAGE_KEY = 'spanda_vidya.theme_preference';

export const DEFAULT_THEME: ThemeMode = 'system';

export const FALLBACK_THEME: 'light' | 'dark' = 'dark';
